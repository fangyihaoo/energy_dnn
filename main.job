#!/bin/csh
#$ -M zhu4@nd.edu     # Email address for job notification
#$ -m abe               # Send mail when job begins, ends and aborts
#$ -q gpu             
#$ -l gpu_card=1
#$ -N deepritz

module load python

python mainPinn.py train --model='FullNet' --functional='poipinn'  --max_epoch=5000 --FClayer=3 --num_input=2 --grid='poiss2dgrid.pt'
